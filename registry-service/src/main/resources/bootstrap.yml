spring:
  application:
    name: registry-service
  profiles:
    active: desarrollo
  cloud:
    config:
      fail-fast: false
      retry:
        maxAttempts: 2000
        maxInterval: 100000
      uri: http://localhost:8081
      username: root
      password: s3cr3t
