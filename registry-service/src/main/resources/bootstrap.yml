spring:
  application:
    name: registry-service
  profiles:
    active: desarrollo
  cloud:
    config:      
      uri: http://config-service:8081
      username: root
      password: s3cr3t
#      fail-fast: true
#      retry:
#        maxAttempts: 2000
#        maxInterval: 100000
