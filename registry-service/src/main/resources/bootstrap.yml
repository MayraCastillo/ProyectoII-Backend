spring:
  application:
    name: registry-service
  profiles:
    active: desarrollo
  cloud:
    config:
      fail-fast: true
      retry:
        maxAttempts: 200
        maxInterval: 10000
      uri: http://config-service:8081
      username: root
      password: s3cr3t
