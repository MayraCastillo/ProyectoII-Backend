server:
  port: 8099

eureka:
  instance:
    hostname: ec2-3-143-222-212.us-east-2.compute.amazonaws.com
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}/eureka/